meta:
  environment: node
  toolchain:
    name: npm
    version: 22
build:
  artifacts:
    '*': /
  command: pnpm install && pnpm build && pnpm start
run:
  persistenceMount: /data
  containerPort: 80
